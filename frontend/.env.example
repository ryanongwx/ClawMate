# Optional: backend API base URL (defaults to http://localhost:4000 in dev)
# VITE_API_URL=http://localhost:4000

# After deploying ChessBetEscrow, set the contract address.
# When set, creating/joining a lobby with bet > 0 will require signing a tx to send the bet to escrow.
# VITE_ESCROW_CONTRACT_ADDRESS=0x...

# Network config — defaults are Monad Mainnet (chain ID 143).
# Uncomment and change to use testnet or another network.
# VITE_CHAIN_ID=0x279F
# VITE_RPC_URL=https://testnet-rpc.monad.xyz
# VITE_CHAIN_NAME=Monad Testnet
# VITE_BLOCK_EXPLORER_URL=https://testnet.monad.xyz

{
  "name": "clawmate-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network monadTestnet",
    "deploy:bnbTestnet": "hardhat run scripts/deploy.js --network bnbTestnet",
    "deploy:bnbMainnet": "hardhat run scripts/deploy.js --network bnbMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}

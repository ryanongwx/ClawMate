PORT=4000
# Comma-separated list of allowed frontend origins (CORS). Multiple frontends can use the same backend.
FRONTEND_URL=http://localhost:5173
# Example for multiple: FRONTEND_URL=https://app1.com,https://app2.com,http://localhost:5173
# Optional: MongoDB URI for lobby persistence (restart-safe). Preferred over Redis if both set.
MONGODB_URI=
# Optional: Redis URL for lobby persistence if MONGODB_URI not set. Leave both unset for in-memory only.
REDIS_URL=
# Monad RPC — mainnet by default; set to https://testnet-rpc.monad.xyz for testnet
MONAD_RPC_URL=https://rpc.monad.xyz
ESCROW_CONTRACT_ADDRESS=
RESOLVER_PRIVATE_KEY=

PORT=4000
FRONTEND_URL=http://localhost:5173
# Optional: MongoDB URI for lobby persistence (restart-safe). Preferred over Redis if both set.
MONGODB_URI=
# Optional: Redis URL for lobby persistence if MONGODB_URI not set. Leave both unset for in-memory only.
REDIS_URL=
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
ESCROW_CONTRACT_ADDRESS=
RESOLVER_PRIVATE_KEY=
